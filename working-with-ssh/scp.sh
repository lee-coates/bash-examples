#!/bin/bash
# These commands are useful for copying files over ssh (scp)

# Assuming your scp user has permissions to the target location, a simple scp takes the form:
scp [path/source/file] [user_name@target_host:target/path]

